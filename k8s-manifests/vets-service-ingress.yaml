apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vets-service-ingress
  namespace: petclinic
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /vet
spec:
  ingressClassName: nginx
  rules:
    - host: www.petclinic-devops.fr
      http:
        paths:
          - path: /api/vet
            pathType: Prefix
            backend:
              service:
                name: vets-service
                port:
                  number: 8083

